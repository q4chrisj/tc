export type Server = {
  version: string;
  versionMajor: number;
  versionMinor: number;
  startTime: string;
  currentTime: string;
  buildNumber: string;
  buildDate: string;
  internalId: string;
  role: string;
  webUrl: string;
};
